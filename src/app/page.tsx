import { LoginButton } from "@app/components/login-button";
import { FC } from "react";

const Home: FC = () => {
  return (
    <main className="main">
      <LoginButton></LoginButton>
    </main>
  );
};

export default Home;
